---
name: Bug report
about: A tool misbehaves, crashes, drops data or gives the wrong exit status
labels: bug
---

**Command** (redact anything private; `--dry-run` output is safe to paste):

```console
$ cat x.txt | jsort "..." --stats
```

**What happened** (paste stderr, including the `--stats` line):

**What you expected:**

**Environment:** `jtools doctor` output, OS, how you installed (`uv tool install jev-tools` / pip).

**Backend:** typesafe / openrouter / vercel / gateway, and the model (`--model`).
